[project]
name = "alogin"
version = "0.0.1"
description = "ALogin - Experimental CLI Authentication Library"
authors = [{ name = "ADEEP AG" }]
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"